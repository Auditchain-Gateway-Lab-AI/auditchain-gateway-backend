$kind: http-request
method: POST
url: '{{baseUrl}}/api/dashboard/verify-data'
order: 10000
headers:
  - key: Content-Type
    value: application/json
  - key: Authorization
    value: 'Bearer {{bearerToken}}'
body:
  type: json
  content: |-
    {
      "resource": "transactions/INV-001",
      "data": {
        "id": "INV-001",
        "amount": 150000,
        "status": "approved"
      }
    }
